l=int(input("Enter from where you want to display:"))
u=int(input("Enter to where you want to display:"))
count=0
for num in range(l,u+1):
    if(num>1):
        for i in range(2,num):
            if(num%i==0):
                break
        else:
            print(num)
        
