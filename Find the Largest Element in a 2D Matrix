matrix = [[3, 8, 1], [14, 7, 20], [6, 15, 2]]
max_element = matrix[0][0]

for row in matrix:
    for num in row:
        if num > max_element:
            max_element = num

print("Largest Element:", max_element)
