matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

rotated = []
for i in range(len(matrix[0])):
    row = []
    for j in range(len(matrix) - 1, -1, -1):
        row.append(matrix[j][i])
    rotated.append(row)

for row in rotated:
    print(row)
